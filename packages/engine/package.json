{
  "name": "@shanku/engine",
  "version": "0.32.0",
  "description": "Shanku model engine: IFC loading (web-ifc, in a worker), element model, and the three.js viewer with Revit-style navigation and ID-buffer picking.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./worker": "./src/ifc/worker.ts",
    "./pyodide": "./src/pyodide.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "bench": "node --experimental-strip-types test/bench.ts"
  },
  "dependencies": {
    "three": "0.186.0",
    "web-ifc": "0.0.77"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/three": "0.186.0",
    "typescript": "^5.6.0",
    "vitest": "^5.0.1"
  }
}
